"""
现有一组数字，需要将其按照题设分成k组子数组，使得子数组的和中的最大值是最小的。给出对应的最小值
解法：
    二分法
"""

from typing import List, Optional

class Solution:
    def splitArray(self, nums:List[int], k:int) -> Optional[int]:
        pass
